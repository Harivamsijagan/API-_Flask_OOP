[project]
name = "fastapi-pr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "requests>=2.34.2",
]

[tool.vercel]
entrypoint = "src.flaskapi_pr.student:app"